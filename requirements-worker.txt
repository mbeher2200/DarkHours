# Async job worker dependencies: the engine runtime (skyfield, boto3, numpy grids —
# no rasterio/GDAL; see docs/RASTERIO_REPLACEMENT.md) — NO FastAPI/uvicorn, the
# worker is SQS-triggered, not HTTP. The deployed worker is a zip Lambda; awslambdaric
# is only exercised when the container image (Dockerfile.worker) is used for the
# Trivy scan or the throwaway in-region perf-test recipe.
-r requirements.txt
awslambdaric==4.0.1
python-json-logger==4.1.0
aws-xray-sdk==2.15.0
