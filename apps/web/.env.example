# Dev-only: the live API origin the Vite dev server proxies API paths to.
# Copy to .env.local (gitignored) and fill in the deployed CloudFront URL.
# Not used in the production build — the SPA calls the API same-origin via relative paths.
VITE_API_ORIGIN=https://your-distribution.cloudfront.net
